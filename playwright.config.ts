// playwright.config.ts

import { devices } from "playwright";

export const use = {
  launchOptions: {
    headless: false, // or false based on your needs
    // other browser launch options
  },
};
